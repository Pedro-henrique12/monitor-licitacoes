version: '3.8'
services:
  db_mysql:
    image: mysql:8.0
    container_name: licitanet_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: licitanet_db
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql